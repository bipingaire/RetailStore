# Production environment — loaded at BUILD TIME by Next.js (next build)
# NEXT_PUBLIC_* vars are baked into the JS bundle, not set at runtime

NEXT_PUBLIC_API_URL=https://indumart.us/api
NEXT_PUBLIC_APP_URL=https://indumart.us
